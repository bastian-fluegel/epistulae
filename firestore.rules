rules_version = '2';
// Nur eigener Nutzerdaten-Zugriff (users/{uid}/…).
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
